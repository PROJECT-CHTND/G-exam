# draft昇格キャンペーン 第2バッチ(supervisedカテゴリ)報告

## 前回指示への対応状況

- バッチ2の予約通り、対象カテゴリを `supervised`(全28枚、draft20枚)とした。
- 規則: 第1バッチの規則 + 鎖基準(①前任の問題→②解決→③残る弱点→④(あれば)後継)を全カードに適用。
- 100%昇格への懸念(較正レビュー指摘)を踏まえ、今回は **kernel** と **spurious-correlation** の2枚を
  根拠つきでdraft残置とした(18/20 = 90%)。理由は本文中「4. draft残置2枚の理由」に記載。

## 1. 対象カテゴリの棚卸し(バッチ開始時点)

| status | 件数 |
|---|---|
| complete(バッチ開始時) | 8 |
| draft(バッチ開始時) | 20 |
| 合計 | 28 |

## 2. 昇格結果

### 2.1 昇格した18枚(id一覧)

multiple-regression, least-squares, lasso, ridge, ar, var, sigmoid, softmax, multiclass,
margin, pruning, ensemble, bagging, bootstrap, boosting, adaboost, gradient-boosting, knn

昇格率: 18/20 = **90%**(第1バッチの100%への較正指摘を受け、根拠が薄い2枚は残置。詳細は4節)

### 2.2 無作為5枚の2質問全文引用(較正確認用)

**ridge(リッジ回帰(L2))**
- bornToSolve: 説明変数間の相関が強い(多重共線性がある)と、最小二乗法で求めた係数が不安定になり、小さなデータの変化で係数が大きく変動してしまう。係数の大きさにペナルティを課して係数を滑らかに縮小し、この不安定さを抑えるために使う。
- beforeAndGap: 通常の線形回帰は係数の大きさを制約しないため多重共線性下で係数が不安定になりやすいが、Ridgeは係数の二乗和(L2)にペナルティを課すことで係数を全体的に縮小し安定させる。ただしLassoのように係数を厳密に0にはしないため、特徴選択の効果は持たない。

**multiclass(多クラス分類)**
- bornToSolve: 通常の分類(二値分類)は2クラスのいずれかを判定する枠組みであり、3クラス以上を判定する問題にはそのままでは使えない。複数のクラスから1つ(または複数)を選ぶ分類問題を扱うために使う。
- beforeAndGap: 二値分類はシグモイドで0/1を判定できるが、多クラス分類ではソフトマックスで各クラスの確率を同時に計算する(二値分類を複数回組み合わせるOne-vs-Restのような方法もある)。

**boosting(ブースティング)**
- bornToSolve: 弱学習器を複数用意しても、単純に平均・多数決するだけでは同じような間違いを繰り返しやすく、間違えやすいデータへの精度が伸びない(未学習が残りやすい)。前の弱学習器の誤りを次の弱学習器が重点的に補正するように逐次追加していくことで、この弱点を解消し表現力を高めるために使う。
- beforeAndGap: バギングは複数モデルを並列・独立に学習して分散を減らすのに対し、ブースティングは前のモデルの誤りを見て次のモデルを逐次的に学習するためバイアス(未学習)を減らす方向に働く。ただし誤りを補正し続けるほど訓練データに過剰適合しやすく、過学習に転じやすい弱点がある。

**bootstrap(ブートストラップサンプリング)**
- bornToSolve: 元のデータセットは1つしかなく、そこから複数の異なる訓練データセットを作る方法がなければ、複数モデルを独立に学習させることができない。元データから重複を許して同じサイズの標本を繰り返し抽出することで、多様な訓練データセットを作るために使う。
- beforeAndGap: 元データをそのまま複数回使うと同じデータセットの繰り返しにしかならないが、ブートストラップサンプリングは重複を許した復元抽出によって元データを近似する擬似的な別データセットを何個も作れる。この手法はバギングの土台であり、ランダムフォレストでも各決定木の学習データを作るために使われる。

**knn(k近傍法(k-NN))**
- bornToSolve: 線形回帰やロジスティック回帰のような手法は、決定境界の形をあらかじめ関数の形(直線・シグモイド等)で仮定してしまうため、境界が複雑に入り組んだデータにはうまく適合しない。未知データに近い訓練データをk個探し、その多数決(分類)や平均(回帰)によって直接予測することで、境界の形をあらかじめ仮定せずに予測するために使う。
- beforeAndGap: 線形回帰やロジスティック回帰は訓練時にパラメータを学習し、推論時はそのパラメータだけを使うが、k近傍法は訓練データそのものを保持し、推論時に毎回距離計算を行う(モデルを学習しない、遅延学習)ため、データ量が増えると推論が遅くなる弱点がある。

## 3. 新規エッジ全列挙+音読(全10本)

| # | from → to | type | 音読文 |
|---|---|---|---|
| 1 | multiple-regression → least-squares | requires | 重回帰分析は最小二乗法を前提とする(係数はOLSで求める)。 |
| 2 | lasso → ridge | contrasts_with | ラッソ回帰(L1・係数を0にする)とリッジ回帰(L2・係数を縮小する)は対比される。 |
| 3 | multiclass → classification-task | is_a | 多クラス分類は分類問題の一種である。 |
| 4 | margin → generalization | used_for | マージン最大化は汎化性能を高めるために使われる。 |
| 5 | bootstrap → random-forest | used_for | ブートストラップサンプリングはランダムフォレストで使われる(各決定木の学習データ作成)。 |
| 6 | adaboost → gradient-boosting | contrasts_with | AdaBoost(重み付け補正)と勾配ブースティング(勾配で補正)は対比される。 |
| 7 | knn → regression-task | is_a | k近傍法は回帰問題の一種として使われる(kNN回帰)。 |
| 8 | ar → linear-regression | is_a | 自己回帰モデル(AR)は線形回帰の一種である(自分自身の過去の値を説明変数とする)。 |
| 9 | var → ar | is_a | ベクトル自己回帰モデル(VAR)は自己回帰モデル(AR)の一種である(多変量拡張)。 |
| 10 | var → spurious-correlation | suffers_from | ベクトル自己回帰モデル(VAR)は疑似相関という問題を抱える(非定常系列同士の回帰で生じやすい)。 |

全10本、独立音読で真であることを確認(from/toの取り違え・過大主張がないか個別に読み直した。特に#10は
既存examHintの「複数系列のAR。疑似相関にも注意。」という記述をエッジ化したものであり新規の主張ではない)。

## 4. draft残置2枚の理由(意図的・根拠つき)

### kernel(カーネル/カーネルトリック)
- 既存エッジ: `kernel --part_of--> svm` の1本のみ。
- 検討した2本目: `kernel --solves--> curse-dimensionality`。カーネル法は高次元への写像を明示せず
  内積だけを計算するため、計算コストの爆発(次元の呪いの**計算面**)は回避できる。しかし
  `curse-dimensionality` カードの定義は「特徴量の次元が増えすぎるとデータが疎になり学習が難しくなる
  **問題**」であり、これは**統計的な**次元の呪い(汎化に必要なデータ量の指数的増大)を指す。カーネル法は
  暗黙的に高次元(無限次元含む)を扱う以上、この統計的側面までは解消しない。両者を同一視するのは
  過大主張(誤答選択肢型の誤り)になるため見送った。
- 新設カード(「線形分離不可能性」等)を作れば解決できるが、本バッチのスコープ(既存draftの昇格)を
  超えるため今回は見送り、draft継続とする。

### spurious-correlation(疑似相関)
- 新規エッジ `var --suffers_from--> spurious-correlation` の1本のみ追加(3節#10)。
- 検討した2本目: 当初 `ar --suffers_from--> spurious-correlation` も候補にしていたが、独立音読で
  誤りと判断し撤回した。疑似相関(spurious regression)は**異なる変量どうし**を回帰した際に、共通の
  トレンドや交絡因子によって見かけの相関が生じる現象であり、ARは単一系列を自分自身の過去に回帰する
  ため、この意味での「疑似」相関は本質的に発生しない(自己相関は定義上「本物」の関係)。そのため
  `ar --is_a--> linear-regression`(3節#8)に差し替え、spurious-correlationへの2本目は追加しなかった。
- solves側の受け皿(差分による定常化・交絡因子の統制など)は本KBの試験スコープ外のカードのみで、
  作成すれば新設扱いになるため見送り。`catastrophic-forgetting`と同型の「対策カード未整備」ケースであり、
  昇格させず正直にWARNING残置とするのが正しい状態と判断した。
- kindは `suffers_from` のto側規約(kind:problem)に合わせて `problem` 化した(この変更自体は独立して
  スキーマ規約への適合であり、昇格判断とは別)。

## 5. WARNING内訳推移

| チェック | 昇格前 | 昇格後 | 差分 | 理由 |
|---|---|---|---|---|
| 孤立ノード | 39 | 36 | -3 | ar・var・spurious-correlationが新規エッジで孤立解消 |
| 未解決問題 | 1 | 2 | +1 | spurious-correlationがkind:problem化されたことで新たに対象化(4節参照。CFと同型の正直な残留) |
| 系譜の鎖(参考情報) | 24 | 24 | 0 | 本バッチはevolves_to系譜に変更なし |
| 網羅性 | 4 | 4 | 0 | 変更なし |
| **合計** | **68** | **66** | **-2** | 単調減少(新規WARNING+1は個別チェック内の正当な増加として明記) |

concepts/relations件数:

| | 昇格前 | 昇格後 | 差分 |
|---|---|---|---|
| concepts | 377 | 377 | 0(新設カードなし) |
| relations | 397 | 407 | +10(3節の新規エッジ) |

## 6. 検証

- `npx tsx scripts/validate.ts` → ERROR 0件 / WARNING 66件(5節参照)
- `npx tsc --noEmit` → エラーなし
- `npm run build` → 成功(dist生成、既存のchunk sizeに関する情報メッセージのみ)

## 7. 次バッチへの申し送り

- バッチ3(対比・系譜がさらに複雑になる想定カテゴリ)着手前に、本パックの人間レビューを待つ。
- kernel/spurious-correlationは「対策・対比カード未整備」の正直なdraft残置として`keyword-backlog.md`等での
  追加記録は不要(理由が本パックに記載済みのため)。将来、線形分離不可能性や時系列定常化系のカードを
  新設する機会があれば再判定する。
